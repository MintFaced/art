# Import report

From `mintface paintings data.xlsx`, read 16/08/2026, 5:57:52 pm.

123 rows, 89 priced, 100 with dimensions, 6 collector names, 24 paintings already with a collector, 7 statuses taken from the sheet over the chain, 9 where the painting comes with the digital work.

Nothing here was guessed at. The settled section records rules you have confirmed and how they were applied. Anything still open is listed at the end.

## Settled

## Available but nothing on offer

Confirmed: these show "Enquire about this artwork" rather than an Acquire button.

- `two-burdens-18` (Two Burdens)
- `two-burdens-30` (Two Burdens)
- `two-burdens-34` (Two Burdens)
- `two-burdens-35` (Two Burdens)

## Status taken from the sheet, against the chain

The painting can change hands without the token moving, which is why these differ. The sheet wins.

- `two-burdens-8` ... sheet says **acquired** (firstladyart.eth), chain says available.
- `two-burdens-11` ... sheet says **acquired** (mintestate.eth), chain says available, applied as **vaulted**.
- `artificial-flowers-3` ... sheet says **acquired** (K Jensen), chain says available.
- `artificial-flowers-6` ... sheet says **acquired** (Liza), chain says available.
- `artificial-flowers-14` ... sheet says **acquired** (K Jensen), chain says available.
- `artificial-flowers-16` ... sheet says **acquired** (K Jensen), chain says available.
- `frogdna-1` ... sheet says **acquired** (S & A Novak), chain says available.

## Prices of zero

Nothing to report.

## Marked no physical, painting price dropped

Confirmed: the note wins over the price columns.

- `artificial-flowers-10` ... note says "no physical available", so the painting price of 1250 and both price of 1400 were dropped. The token is all there is to sell.
- `artificial-flowers-18` ... note says "no physical available", so the painting price of 1250 and both price of 1400 were dropped. The token is all there is to sell.
- `artificial-flowers-19` ... note says "no physical available", so the painting price of 1250 and both price of 1400 were dropped. The token is all there is to sell.

## Collector is the vault, read as vaulted

Confirmed: a work whose collector is mintestate.eth is vaulted, not sold.

- `two-burdens-11` ... collector is mintestate.eth and the sheet said acquired, so it is recorded as vaulted.

## Still open

Nothing. Every question from the last import has an answer.
